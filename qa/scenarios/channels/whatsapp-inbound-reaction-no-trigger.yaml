title: WhatsApp inbound user reaction does not start a fresh run
scenario:
  id: whatsapp-inbound-reaction-no-trigger
  surface: channels
  coverage:
    primary:
      - channels.message-actions
  execution:
    kind: flow
    channel: whatsapp
    timeoutMs: 90000
    retryCount: 1
    suiteIsolation: isolated
flow:
  module: ./live-transports/whatsapp/scenario-runtime.js
  call: runWhatsAppScenario
  args:
    - { expr: whatsappScenarioContext }
    - expr: "(await qaImport('./live-transports/whatsapp/whatsapp-live.scenario-implementations.user-path.js')).whatsappQaInboundReactionNoTriggerScenario"
