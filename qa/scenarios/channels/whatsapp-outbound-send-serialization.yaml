title: WhatsApp parallel Gateway sends deliver every outbound message
scenario:
  id: whatsapp-outbound-send-serialization
  surface: channels
  coverage:
    primary:
      - whatsapp.outbound-text-sends
  execution:
    kind: flow
    channel: whatsapp
    timeoutMs: 90000
    retryCount: 1
    suiteIsolation: isolated
flow:
  module: ./live-transports/whatsapp/scenario-runtime.js
  call: runWhatsAppScenario
  args:
    - { expr: whatsappScenarioContext }
    - expr: "(await qaImport('./live-transports/whatsapp/whatsapp-live.scenario-implementations.capabilities.js')).whatsappQaOutboundSendSerializationScenario"
