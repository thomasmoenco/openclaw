title: WhatsApp direct Gateway send delivers media to a group
scenario:
  id: whatsapp-group-outbound-media
  surface: channels
  coverage:
    primary:
      - whatsapp.outbound-image
      - whatsapp.outbound-document
  execution:
    kind: flow
    channel: whatsapp
    timeoutMs: 120000
    retryCount: 1
    suiteIsolation: isolated
flow:
  module: ./live-transports/whatsapp/scenario-runtime.js
  call: runWhatsAppScenario
  args:
    - { expr: whatsappScenarioContext }
    - expr: "(await qaImport('./live-transports/whatsapp/whatsapp-live.scenario-implementations.capabilities.js')).whatsappQaGroupOutboundMediaScenario"
